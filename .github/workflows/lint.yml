name: Lint
on:
  pull_request:
  merge_group:

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
